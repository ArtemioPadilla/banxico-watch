# Banxico Watch

Generado con create-inceptor-app (archetype: `static`).

```bash
npm install
npm run dev      # http://localhost:4321
npm run check    # gate: astro check + type-check + tests + build
```

Recetas (auth, IA, hosting): ver el repo scaffold, carpeta `docs/recipes/`.
